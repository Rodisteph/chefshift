export const metadata = {
  title: 'Mijn profiel',
  robots: { index: false, follow: false },
}

export default function ProfielLayout({ children }: { children: React.ReactNode }) {
  return children
}
