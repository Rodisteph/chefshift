export const metadata = {
  title: 'Nieuwe shift plaatsen',
  robots: { index: false, follow: false },
}

export default function NieuweShiftLayout({ children }: { children: React.ReactNode }) {
  return children
}
