export const metadata = {
  title: 'Inloggen',
  description: 'Log in op ChefShift en beheer je shifts, sollicitaties en betalingen.',
  alternates: { canonical: '/login' },
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
