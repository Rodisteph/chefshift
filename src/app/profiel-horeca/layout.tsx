export const metadata = {
  title: 'Mijn profiel',
  robots: { index: false, follow: false },
}

export default function ProfielHorecaLayout({ children }: { children: React.ReactNode }) {
  return children
}
